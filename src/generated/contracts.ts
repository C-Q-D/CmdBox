// 此文件由 Rust serde DTO 生成；请勿手工编辑。

// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * IPC 调用失败时返回的稳定错误对象。
 */
export type ApiError = {
/**
 * 供前端稳定分支处理的错误码。
 */
code: string,
/**
 * 面向用户或开发者的中文错误说明。
 */
message: string,
/**
 * 参数校验错误直接关联的 Parameter key；其他错误省略。
 */
parameterKey?: string,
/**
 * 参数或内部模板错误的稳定原因码；其他错误省略。
 */
detailCode?: string, };

/**
 * Boolean 参数及其明确默认值。
 */
export type BooleanParameterDefinition = {
/**
 * optional 参数未提交时使用的明确布尔默认值。
 */
defaultValue: boolean,
/**
 * 在一个 Command Block 内唯一且可被受限模板引用的稳定 key。
 */
key: string,
/**
 * Command Workspace 展示的参数名称。
 */
label: string,
/**
 * Command Workspace 可选展示的参数说明。
 */
description: string | null,
/**
 * 请求是否必须显式提交该 key；默认值不会替代 required 输入。
 */
required: boolean,
/**
 * 后续持久化层是否允许记忆该参数；本模块不执行持久化。
 */
remember: boolean, };

/**
 * 取消调用返回的非终态事实。
 */
export type CancelExecutionResponse = {
/**
 * 本次调用是否首次接受取消请求。
 */
accepted: boolean,
/**
 * Execution 当前可观察状态；不存在或已终止时为 `None`。
 */
state: IpcActiveExecutionState | null, };

/**
 * Command Workspace 参数表单使用的公开 Command Block 详情字段白名单。
 *
 * Details 在 Summary 字段之外只增加 Parameter Definition；固定模板继续保留在 Rust Core，
 * 前端不能读取、修改或回传模板以绕过 Planner。
 */
export type CommandBlockDetails = {
/**
 * Command Block 的稳定身份。
 */
id: string,
/**
 * Command Workspace 展示的名称。
 */
name: string,
/**
 * Command Workspace 展示的用途说明。
 */
description: string,
/**
 * Built-in 或 User 来源身份。
 */
origin: CommandOrigin,
/**
 * Definition 声明的稳定 Runner 类型。
 */
runner: RunnerType,
/**
 * normal 或 destructive 风险语义。
 */
riskLevel: RiskLevel,
/**
 * 当前 Definition revision。
 */
revision: number,
/**
 * 按统一 Command Workspace 顺序返回的类型化参数定义。
 */
parameters: Array<ParameterDefinition>, };

/**
 * Command Workspace 列表使用的公开 Command Block 字段白名单。
 *
 * DTO 故意不含模板、可执行文件、Runner options、工作目录或环境变量，避免列表 IPC 将
 * Rust Core 内部执行定义泄露给 WebView。
 */
export type CommandBlockSummary = {
/**
 * Command Block 的稳定身份。
 */
id: string,
/**
 * Command Workspace 展示的名称。
 */
name: string,
/**
 * Command Workspace 展示的用途说明。
 */
description: string,
/**
 * Built-in 或 User 来源身份。
 */
origin: CommandOrigin,
/**
 * Definition 声明的稳定 Runner 类型。
 */
runner: RunnerType,
/**
 * normal 或 destructive 风险语义。
 */
riskLevel: RiskLevel,
/**
 * 当前 Definition revision。
 */
revision: number, };

/**
 * Command Block 的来源身份。
 */
export type CommandOrigin = "builtin" | "user";

/**
 * 专属 Tauri Channel 上的完整事件流。
 */
export type ExecutionStreamEvent = { "event": "started", "data": {
/**
 * 当前 Execution UUID。
 */
executionId: string,
/**
 * IPC 转发器分配的事件级顺序。
 */
sequence: number, } } | { "event": "output", "data": {
/**
 * 当前 Execution UUID。
 */
executionId: string,
/**
 * IPC 转发器分配的事件级顺序。
 */
sequence: number,
/**
 * 保持协调器观察顺序的纯文本片段。
 */
fragments: Array<IpcOutputFragment>,
/**
 * 当前 Batch 之前因有界队列压力被丢弃的字节数。
 */
droppedBytesBefore: number, } } | { "event": "finished", "data": {
/**
 * 当前 Execution UUID。
 */
executionId: string,
/**
 * IPC 转发器分配的事件级顺序。
 */
sequence: number,
/**
 * Runner 根进程的原始 Exit Code。
 */
exitCode: number,
/**
 * Rust Core 按 Command Block Policy 生成的业务结果。
 */
outcome: Outcome,
/**
 * Rust Core 从 Resume 到终态的毫秒数。
 */
durationMs: number,
/**
 * 尚未随 Output Batch 报告的丢弃字节数。
 */
droppedOutputBytes: number, } } | { "event": "cancelled", "data": {
/**
 * 当前 Execution UUID。
 */
executionId: string,
/**
 * IPC 转发器分配的事件级顺序。
 */
sequence: number,
/**
 * 取消没有自然完成的业务结果，固定为 `none`。
 */
outcome: Outcome,
/**
 * Rust Core 从 Resume 到终态的毫秒数。
 */
durationMs: number,
/**
 * 尚未随 Output Batch 报告的丢弃字节数。
 */
droppedOutputBytes: number, } } | { "event": "failed", "data": {
/**
 * 当前 Execution UUID。
 */
executionId: string,
/**
 * IPC 转发器分配的事件级顺序。
 */
sequence: number,
/**
 * Rust Core 返回的稳定失败说明。
 */
message: string,
/**
 * Core 内部失败不代表命令业务失败，固定为 `none`。
 */
outcome: Outcome,
/**
 * Rust Core 从 Resume 到终态的毫秒数。
 */
durationMs: number,
/**
 * 尚未随 Output Batch 报告的丢弃字节数。
 */
droppedOutputBytes: number, } };

/**
 * Folder 参数的绝对目录约束。
 */
export type FolderParameterDefinition = {
/**
 * 是否要求规范化后的目标根对象当前存在且是目录。
 */
mustExist: boolean,
/**
 * optional 参数未提交时使用的绝对目录文本。
 */
defaultValue: string | null,
/**
 * 在一个 Command Block 内唯一且可被受限模板引用的稳定 key。
 */
key: string,
/**
 * Command Workspace 展示的参数名称。
 */
label: string,
/**
 * Command Workspace 可选展示的参数说明。
 */
description: string | null,
/**
 * 请求是否必须显式提交该 key；默认值不会替代 required 输入。
 */
required: boolean,
/**
 * 后续持久化层是否允许记忆该参数；本模块不执行持久化。
 */
remember: boolean, };

/**
 * Folders 参数的绝对目录数组和数量约束。
 */
export type FoldersParameterDefinition = {
/**
 * 是否要求每个规范化目标根对象当前存在且是目录。
 */
mustExist: boolean,
/**
 * 可接受的最少目录项数量。
 */
minItems: number | null,
/**
 * 可接受的最多目录项数量。
 */
maxItems: number | null,
/**
 * optional 参数未提交时使用的绝对目录文本数组。
 */
defaultValue: Array<string> | null,
/**
 * 在一个 Command Block 内唯一且可被受限模板引用的稳定 key。
 */
key: string,
/**
 * Command Workspace 展示的参数名称。
 */
label: string,
/**
 * Command Workspace 可选展示的参数说明。
 */
description: string | null,
/**
 * 请求是否必须显式提交该 key；默认值不会替代 required 输入。
 */
required: boolean,
/**
 * 后续持久化层是否允许记忆该参数；本模块不执行持久化。
 */
remember: boolean, };

/**
 * 可通过 IPC 观察的 Active Execution 状态。
 */
export type IpcActiveExecutionState = "running" | "cancelling";

/**
 * 一个按 Output Coordinator 观察顺序生成的纯文本片段。
 */
export type IpcOutputFragment = {
/**
 * Output Coordinator 分配的片段级顺序，不与事件级顺序混用。
 */
fragmentSequence: number,
/**
 * 片段所属标准流。
 */
stream: IpcOutputStream,
/**
 * 已在 Rust 侧增量解码的不可信纯文本。
 */
text: string, };

/**
 * 输出文本来自哪个标准流。
 */
export type IpcOutputStream = "stdout" | "stderr";

/**
 * Number 参数的有限值、范围和步长约束。
 */
export type NumberParameterDefinition = {
/**
 * optional 参数未提交时使用的默认数字。
 */
defaultValue: number | null,
/**
 * 可接受的最小值，包含边界。
 */
min: number | null,
/**
 * 可接受的最大值，包含边界。
 */
max: number | null,
/**
 * 相对 `min`（没有 `min` 时相对零）的正有限步长。
 */
step: number | null,
/**
 * 在一个 Command Block 内唯一且可被受限模板引用的稳定 key。
 */
key: string,
/**
 * Command Workspace 展示的参数名称。
 */
label: string,
/**
 * Command Workspace 可选展示的参数说明。
 */
description: string | null,
/**
 * 请求是否必须显式提交该 key；默认值不会替代 required 输入。
 */
required: boolean,
/**
 * 后续持久化层是否允许记忆该参数；本模块不执行持久化。
 */
remember: boolean, };

/**
 * Rust Core 发布给调用方的稳定业务结果。
 */
export type Outcome = "none" | "success" | "warning" | "partialFailure" | "failure";

/**
 * 当前 CMD-02 原子支持的六类 Parameter Definition。
 */
export type ParameterDefinition = { "type": "text" } & TextParameterDefinition | { "type": "number" } & NumberParameterDefinition | { "type": "boolean" } & BooleanParameterDefinition | { "type": "select" } & SelectParameterDefinition | { "type": "folder" } & FolderParameterDefinition | { "type": "folders" } & FoldersParameterDefinition;

/**
 * 不依赖 `serde_json` 的完整 JSON 值语义，用于在 Rust Core 内继续执行类型化校验。
 */
export type ParameterValue = null | boolean | number | string | Array<ParameterValue> | { [key in string]: ParameterValue };

/**
 * Preview 请求，只允许提交业务身份、期望 revision 和结构化参数。
 */
export type PreviewCommandRequest = {
/**
 * 用户当前打开的 Command Block ID。
 */
commandBlockId: string,
/**
 * 用户基于的 Definition revision。
 */
expectedRevision: number,
/**
 * 尚未信任、必须由 Rust Core 验证的结构化参数值。
 */
parameterValues: { [key in string]: ParameterValue }, };

/**
 * 用户可以检查且后续 Run 必须完整复验的 Command Preview。
 */
export type PreviewCommandResponse = {
/**
 * 当前 Preview 对应的 Command Block ID。
 */
commandBlockId: string,
/**
 * 当前 Preview 对应的 Definition revision。
 */
revision: number,
/**
 * 由当前 Definition 声明且由 Rust 解析的 Runner 类型。
 */
runner: RunnerType,
/**
 * Rust 规范化参数的有界可读摘要。
 */
parameterSummaries: Array<PreviewParameterSummary>,
/**
 * 不含编码前导的有界可读 Runner 脚本文本。
 */
previewText: string,
/**
 * 完整最终 Artifact 字节数；编码和 BOM 由当前 Runner 契约决定。
 */
fullSizeBytes: number,
/**
 * `preview_text` 是否因展示上限而被截断。
 */
truncated: boolean,
/**
 * 当前 Definition 的稳定风险语义。
 */
riskLevel: RiskLevel,
/**
 * 当前 normal Built-in 的明确用户动作文案。
 */
actionLabel: string,
/**
 * Rust Core 生成的结构化安全结论。
 */
safety: PreviewSafetyDecision,
/**
 * high-risk Preview 需要的确认要求；普通和 normal Command 为空。
 */
confirmationRequirement?: PreviewConfirmationRequirement,
/**
 * destructive 目标身份列表的稳定凭据；normal Command 为空。
 */
targetIdentityHash?: string,
/**
 * 覆盖完整 Canonical Execution Spec 的 64 字符 SHA-256。
 */
executionSpecHash: string, };

/**
 * Rust Core 对 high-risk destructive 操作给出的版本化确认要求。
 */
export type PreviewConfirmationRequirement = {
/**
 * 进入 Execution Spec 的确认语义版本。
 */
version: number,
/**
 * 用户必须精确提交的固定短语。
 */
phrase: string, };

/**
 * Rust Core 为一个 Parameter Definition 生成的有界规范化摘要。
 */
export type PreviewParameterSummary = {
/**
 * 对应 Parameter Definition 的稳定 key。
 */
parameterKey: string,
/**
 * 直接来自当前 Definition 的用户可读名称。
 */
label: string,
/**
 * 已规范化值的有界可读文本，顺序与 Rust 规范化结果一致。
 */
displayValues: Array<string>,
/**
 * 未受展示截断影响的完整值数量；标量存在时为一。
 */
totalCount: number,
/**
 * 值数量或任一显示值是否因上限而截断。
 */
truncated: boolean, };

/**
 * Rust Core 对当前完整规范作出的结构化安全结论。
 */
export type PreviewSafetyDecision = {
/**
 * 当前 Safety Policy 的结构化结果。
 */
state: PreviewSafetyState,
/**
 * 可选的用户可读结论摘要。
 */
summary: string | null,
/**
 * 有顺序的具体警告；当前 normal Built-in 始终为空。
 */
warnings: Array<PreviewSafetyWarning>, };

/**
 * Safety Decision 当前可序列化的稳定状态。
 */
export type PreviewSafetyState = "notApplicable" | "passed" | "warning" | "blocked";

/**
 * 一条由 Rust Safety Guard 生成的稳定警告；当前 normal Built-in 不返回任何项。
 */
export type PreviewSafetyWarning = {
/**
 * 前端可以稳定匹配的警告码。
 */
code: string,
/**
 * 面向用户的警告文本。
 */
message: string, };

/**
 * Command Block 的稳定风险语义。
 */
export type RiskLevel = "normal" | "destructive";

/**
 * Command Block 启动成功后的最小响应。
 */
export type RunCommandResponse = {
/**
 * 新建 Execution 的 UUID 字符串。
 */
executionId: string, };

/**
 * 当前 Windows MVP 可由 Command Block 声明的 Runner。
 */
export type RunnerType = "windowsPowerShell" | "cmd";

/**
 * high-risk destructive Run 的窄确认响应。
 */
export type SafetyConfirmationResponse = {
/**
 * 当前版本固定要求的确认短语。
 */
phrase: string, };

/**
 * Select 参数的固定选项集合。
 */
export type SelectParameterDefinition = {
/**
 * 可提交的固定字符串选项；比较区分大小写。
 */
options: Array<string>,
/**
 * optional 参数未提交时使用的固定选项。
 */
defaultValue: string | null,
/**
 * 在一个 Command Block 内唯一且可被受限模板引用的稳定 key。
 */
key: string,
/**
 * Command Workspace 展示的参数名称。
 */
label: string,
/**
 * Command Workspace 可选展示的参数说明。
 */
description: string | null,
/**
 * 请求是否必须显式提交该 key；默认值不会替代 required 输入。
 */
required: boolean,
/**
 * 后续持久化层是否允许记忆该参数；本模块不执行持久化。
 */
remember: boolean, };

/**
 * Text 参数的长度和默认值约束。
 */
export type TextParameterDefinition = {
/**
 * optional 参数未提交时使用的默认文本。
 */
defaultValue: string | null,
/**
 * 按 Unicode scalar value 计数的最小长度。
 */
minLength: number | null,
/**
 * 按 Unicode scalar value 计数的最大长度。
 */
maxLength: number | null,
/**
 * 仅供 Command Workspace 展示的输入提示。
 */
placeholder: string | null,
/**
 * 在一个 Command Block 内唯一且可被受限模板引用的稳定 key。
 */
key: string,
/**
 * Command Workspace 展示的参数名称。
 */
label: string,
/**
 * Command Workspace 可选展示的参数说明。
 */
description: string | null,
/**
 * 请求是否必须显式提交该 key；默认值不会替代 required 输入。
 */
required: boolean,
/**
 * 后续持久化层是否允许记忆该参数；本模块不执行持久化。
 */
remember: boolean, };

/**
 * Run 复验请求，只增加用户确认的完整 Execution Spec Hash。
 */
export type VerifyRunRequest = {
/**
 * 用户要执行的 Command Block ID。
 */
commandBlockId: string,
/**
 * 用户 Preview 时确认的 Definition revision。
 */
expectedRevision: number,
/**
 * 必须重新验证、规范化和渲染的原始结构化参数值。
 */
parameterValues: { [key in string]: ParameterValue },
/**
 * Preview 返回且用户已经确认的完整 Execution Spec SHA-256。
 */
executionSpecHash: string,
/**
 * destructive high-risk Preview 要求的明确确认响应；normal Command 省略。
 */
safetyConfirmation?: SafetyConfirmationResponse,
/**
 * Preview 返回的目标身份凭据；用于在完整 Hash 比较前识别目标被替换。
 */
targetIdentityHash?: string, };
