# CmdBox 项目工作台

## 领航状态

- 当前阶段：`CMD-04` 安全快速永久删除实现
- 项目路线：完整路线
- 项目根目录：`F:\workspace\projects\CmdBox`
- 最近更新：2026-08-24
- 最近完成：`CMD-03` 五个原子；版本化 Outcome Policy、Typed IPC、Workspace 分离展示和安全真实宿主矩阵均已验收
- 当前切片：[CMD-04 安全快速永久删除原子计划](../开发计划/原子开发计划-CMD-04安全快速永久删除.md)；前两个原子已完成，当前原子 `CMD04-EXECUTOR-01`
- 当前授权：连续实施 `CMD-04` 的八个原子并逐项验证、提交和推送；不包含 `CMD-05`、持久化或其他后续产品单元
- 当前阶段必需产物：目标根安全与身份、可信删除 Executor、Single Flight、逐目标契约、Workspace、真实 Windows/Tauri 安全矩阵、Hero Benchmark 和完整测试清单
- 下一关口：CMD-04 全部完成后停止，等待用户检查并明确是否进入 `CMD-05`
- 恢复入口：先阅读根目录 `AGENTS.md`、本工作台、[CMD-04 原子计划](../开发计划/原子开发计划-CMD-04安全快速永久删除.md)和[测试与验收](../testing/测试与验收.md)，从唯一 `in_progress` 原子继续

## 项目定义

CmdBox 当前定位、用户、MVP、非目标、核心流程、业务规则和验收条件统一见[产品需求](../product/产品需求.md)，本工作台不重复维护正文。

## 验收标准

- AC-01 至 AC-09 的定义统一见[产品需求](../product/产品需求.md)。
- 验证方式、场景和发布门禁统一见[测试与验收](../testing/测试与验收.md)。
- 当前状态：`CMD-01` 至 `CMD-03` 已验证；Hero Delete 已进入实现，但尚未取得 CMD-04 的真实删除与目标级完成证据。

## 待决问题队列

| 编号 | 问题 | 决定者 | 目标阶段 | 当前只需确认 | 建议 | 状态 |
|---|---|---|---|---|---|---|
| Q-01 | M0 使用系统自带 Windows PowerShell 的具体可执行文件解析方式 | AI 检查 | EXEC-BE-01 | 否 | 通过 System32 确定解析，不依赖可变 PATH | 已解决 |
| Q-02 | CMD 中文输出的最终解码策略 | AI 实验 | CMD-02 / 发布门禁 | 否 | 当前内置范围使用 UTF-8 无 BOM Batch、CP65001 与严格增量 UTF-8 解码；外部程序编码不在本契约内 | 已解决 |
| Q-03 | CmdBox 可接受的具体性能预算阈值 | AI 实验后用户确认 | CMD-03 验收 | 否 | 先记录直接终端基线与 CmdBox Overhead，再用实测决定阈值 | 已推迟 |
| Q-04 | Bash/Zsh 与非 Windows 平台进入哪个后续版本 | 用户 | MVP 完成后 | 否 | 不进入当前 Windows MVP | 已推迟 |

## 阶段与产物

详细台账见：[项目阶段记录](项目阶段记录.md)

## 风险、依赖与假设

| 项目 | 类型 | 处理阶段或方式 | 状态 |
|---|---|---|---|
| 永久删除造成不可逆数据损失 | 高风险 | Safety Policy、两次校验、路径身份和真实 Windows 测试 | 已登记 |
| Preview 与 Run 状态漂移 | 高风险 | Revision、`executionSpecHash`、Path Fingerprint | 已登记 |
| Cancel 只终止父进程 | 高风险 | Windows Job Object 和整棵进程树测试 | 已登记 |
| 大量输出拖慢命令或压垮 UI | 重要风险 | Rust Aggregator/Batch/Channel 已实现；前端当前为 512 KiB、2048 非空 Chunk 的有界直接渲染，虚拟化 Viewer 留待后续性能单元 | 部分已处理 |
| 高频启动和重建拖慢开发 | 开发效率 | Vite HMR、Tauri Watch、Cargo Incremental、Compose Watch 和依赖指纹缓存 | 已处理并实测 |
| 产品交付单元遗漏全局安全契约 | 高风险规划 | 隔离对抗式审查并修订可验收卡 | 已通过 |

## 实现切片

以活动计划 [CmdBox MVP 产品拆分](../开发计划/产品拆分-CmdBox-MVP.md) 为准。

## 决定与变更

| 日期 | 决定或变更 | 原因 | 影响 |
|---|---|---|---|
| 2026-08-22 | MVP 收缩为 Windows First 的一次性命令执行器 | 用户核心痛点是超大文件夹快速删除，不需要交互终端 | PTY、SSH 和 Workflow 移出 MVP |
| 2026-08-22 | Hero Use Case 为快速永久删除多个文件夹 | 最能验证 CmdBox 相对笔记和 Terminal 的独立价值 | 安全、性能、取消和 Outcome 成为首要约束 |
| 2026-08-22 | 同一职责只保留最终权威文档 | 用户要求去除 v0.1/v0.2 等重复版本，只维护当前最终内容 | 产品、技术、安全和性能文档按职责收敛，旧版本删除 |
| 2026-08-22 | 正式开发采用后端先行并设置前端原型门禁 | 用户要求先完成后端，任何前端开发前必须先确认界面原型 | 当前只实施 Rust 后端；后端完成不等于 `CMD-01` 产品单元完成 |
| 2026-08-23 | 所有命令使用统一 Command Workspace，内容由类型化定义和后端状态驱动 | 不同命令的参数、风险和结果不同，但不能为每条命令维护独立页面 | 先确认内容模型，再重新设计视觉原型；MVP 不引入任意 UI DSL |
| 2026-08-23 | 前端视觉采用 `editorial-field-notes` 方案 1 | 用户在三套同风格方案中明确选择第 1 张 | 以项目内视觉原型为真值；先实现无副作用前端原型，再单独规划真实 IPC |
| 2026-08-24 | CMD-03 只交付 Outcome 基础契约，不伪造目标事实 | 真实目标事实只能由后续删除 Executor 产生 | 普通/特殊 Exit Code 和 Cancel 在 CMD-03 验收；真实 partialFailure 归 CMD-04 |
| 2026-08-24 | CMD-04 默认命令必须晚于真实 Windows 安全门禁 | 永久删除不可逆，feature 内部完成不等于默认产品可交付 | 前六个原子保持 `delete-validation`，完整宿主矩阵通过后才提升默认 Registry |

## 验证摘要

- 已通过：Node/pnpm/Rust/MSVC/WebView2/Docker 主机检查、冻结依赖安装、前端构建与测试、Rust 检查与测试、Docker Compose Watch/HMR、真实 Windows Tauri 窗口、前端 HMR、Rust Watch、精确重启和精确停止。
- 新增通过：Command Workspace Ready 状态、搜索、Preview 失效、恢复、disabled 和无副作用说明对话框；同尺寸视觉 QA、窄窗口检查、控制台检查、Lighthouse Accessibility 100；完整 `pnpm check` 通过。
- 新增通过：固定无破坏任务已从真实 React Workspace 经 Typed IPC 进入 Rust Core；自然结束、整树取消、取消竞态、慢消费、Channel 断开、Core 强退和不可信文本场景均取得真实宿主证据。
- 新增通过：真实 Tauri 仅显示一层自定义标题栏；拖动、最小化、最大化/还原、双击和关闭在 Windows 宿主验证通过，窗口修复后的 `CMD-01` 执行与进程树清理无回归。
- 新增通过：六类 Typed Parameter、受限模板、完整 `executionSpecHash`/Run 复验、PowerShell/CMD Runner 与前端生成契约；Gateway 只开放 list/get/preview/run/cancel 五个业务 IPC，目录选择只开放 Dialog Open 权限。
- 新增通过：统一参数表单与 Rust Preview 已接线；参数修改、双击、A→B→A、迟到响应、字段错误、Safety 四态、纯文本、截断和无参数 Strict Mode 流程均有前端自动化证据，Preview 阶段未调用通用 Run 或创建 Channel。
- 新增通过：完整前端回归为 7 个文件、97 项测试；一次性 Confirmed Preview、同步双击 Run、Execution ID/sequence/generation、唯一终态、全部 Run 拒绝、Cancel 竞态、五种 Outcome、512 KiB/2048 非空 Chunk 与预响应 event/fragment/byte 上限均已覆盖。
- 新增通过：默认真实 Tauri 分别完成 PowerShell/CMD 六类参数回显，均取得有效 Execution UUID、精确 stdout、空 stderr、唯一 Finished、Exit Code 0 与测试目录未变化证据；显式 `ui-validation` 构建完成 Preview → Run → UI Cancel → Cancelling → 唯一 Cancelled，结束后默认列表恢复为两个正式 Built-in。
- 新增通过：`1487`、`980`、`680` 三档响应式、键盘焦点和单层 Windows 标题栏检查通过；全部真实命令只包含回显、参数展示和最多 5 秒短等待，未运行删除、移动、覆盖、网络、注册表或其他危险命令。本轮未取得独立 WebView console attachment，已在验收清单中单列限制；真实流程没有可见错误提示或 Vite overlay。
- 新增通过：版本化 Outcome Policy 已进入 Canonical Spec；真实 Tauri 显示普通 `9 → 失败`、特殊 `1 → 成功`、`3 → 警告`、`8 → 失败`，短等待 Cancel 为“未生成”且无 Exit Code；默认 Registry 仍严格为两个正式 Echo。
- 尚未完成：CMD-04 永久删除与真实目标级结果正在实施；持久日志、虚拟化 Viewer、History、持久化和完整 MVP 发布门禁仍未开始。
- 当前执行边界：连续完成 `CMD-04` 后停在 `CMD-05` 前，不进入持久化。
